using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerSpriteGenerator : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //DefaultSprite = Reasources.Load("costumes/default") as Sprite;
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
